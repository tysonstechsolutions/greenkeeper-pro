Make sure everything works with no errors before you present it to me. Take as long as you need — correctness over speed.

## Git workflow
After finishing a task, commit the changes with a clear message — automatically, without being asked. Only push when Tyson explicitly asks for a push in that conversation; never push on your own initiative. (A local PreToolUse hook in `.claude/settings.json` makes every push ask Tyson for approval as a backstop.)