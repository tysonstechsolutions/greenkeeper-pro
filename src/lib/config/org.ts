/**
 * Organization identity. Single source of truth for the course name so
 * generated documents, flyers, letters, and agreements are consistent and
 * never hardcode it in multiple places. "VMGC" throughout the app is the
 * abbreviation of this name.
 */
export const COURSE_NAME = "Veterans Memorial Golf Course";
